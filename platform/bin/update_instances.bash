#! /usr/bin/env bash

# use the browser service to get a list of known services

# if the service version does not match the current version
#   - shell to the instance and execute the update_instance.bash script
